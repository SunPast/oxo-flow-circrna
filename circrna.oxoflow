# circRNA Detection Pipeline
# Built on oxo-flow engine
#
# QUICK START:
#   1. Set reference_dir below to your genome directory
#   2. Place FASTQ files in raw/
#   3. oxo-flow run circrna.oxoflow -j 16
#
# Indexes are auto-built on first run. Environments are auto-created.

[workflow]
name = "circrna"
version = "1.0.0"
description = "circRNA detection and analysis pipeline"
author = "oxo-flow-circrna"
format_version = "1.0"
sample_pattern = "raw/{sample}_1.fastq.gz"

[config]
# === The only path you need to set ===
# All indexes and reference paths are auto-derived from this directory.
# Expected layout:
#   {reference_dir}/genome.fa
#   {reference_dir}/genes.gtf
reference_dir = "/data/references/GRCh38"

# === Auto-derived from reference_dir — override any of these if your layout differs ===
reference_fasta = "{config.reference_dir}/genome.fa"
gene_annotation = "{config.reference_dir}/genes.gtf"
circexplorer2_ref = "{config.reference_dir}/hg38_ref.txt"
ciriquant_config = "{config.reference_dir}/CIRIquant.yml"

# Engine-injected at runtime: merged, sorted sample list from raw/ discovery.
# The empty string is a validate-time placeholder — discovered samples are appended.
samples_list = ""

[defaults]
threads = 8
memory = "16G"

# === Indexes ===
# Auto-built on first run from reference_dir. No manual steps needed.
# To customize or add indexes, declare [[references]] blocks explicitly.
# See: https://traitome.github.io/oxo-flow/reference/workflow-format/#references

# === Sub-workflows ===
[[include]]
path = "rules/qc.oxoflow"

[[include]]
path = "rules/callers.oxoflow"

[[include]]
path = "rules/annotation.oxoflow"

[[include]]
path = "rules/aggregation.oxoflow"

[[include]]
path = "rules/report.oxoflow"
